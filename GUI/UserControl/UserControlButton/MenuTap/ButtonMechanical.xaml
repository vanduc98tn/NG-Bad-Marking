<UserControl x:Class="GUI.ButtonMechanical"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GUI"
             mc:Ignorable="d"
             
             Background="{Binding Background, RelativeSource={RelativeSource AncestorType=UserControl}}"
             Tag="{Binding Tag, RelativeSource={RelativeSource AncestorType=UserControl}}"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Button Click="btnJigInforMenu_Click" TouchDown="btnJigInforMenu_TouchDown" x:Name="btnJigInforMenu" Grid.Row="1" Margin="1,1,1,1"
                    Style="{StaticResource ButtonHeader}"
                    FontSize="20"
                Background="{Binding Background, RelativeSource={RelativeSource AncestorType=UserControl}}"
                Tag="{Binding Tag, RelativeSource={RelativeSource AncestorType=UserControl}}"
                Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=UserControl}}">
            <StackPanel Orientation="Horizontal" Width="1000">
                <Rectangle HorizontalAlignment="Left" Width="15" Height="48" Fill="{Binding RectangleFill, RelativeSource={RelativeSource AncestorType=UserControl}}" />
                <TextBlock HorizontalAlignment="Left" VerticalAlignment="Center" Padding="10,0,0,0"><Run Text="{Binding Content, RelativeSource={RelativeSource AncestorType=UserControl}}"/></TextBlock>
            </StackPanel>
        </Button>
        
    </Grid>
</UserControl>
