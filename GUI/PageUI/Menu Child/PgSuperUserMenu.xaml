<Page x:Class="GUI.PgSuperUserMenu"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:GUI"
      mc:Ignorable="d" 
      d:DesignHeight="840" d:DesignWidth="1300"
      Title="PgSuperUserMenu">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition Width="8*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <UniformGrid Name="ugridJamList" Columns="3">
                    <UniformGrid.Resources>
                        <Style TargetType="Button" x:Key="mButton">
                            <Setter Property="FontSize" Value="20"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="Foreground" Value="DodgerBlue"/>
                        </Style>
                    </UniformGrid.Resources>
                    <!-- Các nút sẽ tự động được thêm vào đây -->
                </UniformGrid>
            </ScrollViewer>
        </Grid>
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition></RowDefinition>
                <RowDefinition></RowDefinition>
                <RowDefinition></RowDefinition>
                <RowDefinition Height="10*"></RowDefinition>
            </Grid.RowDefinitions>
            <Button Margin="2" Grid.Row="0" Content="Animation"  Click="AnimationButton_Click" Grid.Column="1"/>
            <Button Margin="2" Grid.Row="1" Content="Load Image" Click="LoadImageButton_Click"  Grid.Column="1"/>
            <Button Margin="2" Grid.Row="2" Content="Save Image" Click="Save_Click"  Grid.Column="1"/>
        </Grid>
        <Canvas Width="600" Height="270" Grid.Column="2" x:Name="canvas" Background="LightGray"
                MouseDown="Canvas_MouseDown"
                MouseMove="Canvas_MouseMove"
                MouseUp="Canvas_MouseUp">

            <Canvas.Resources>
                <Style TargetType="Ellipse">
                    <Setter Property="Stroke" Value="Red"/>
                    <Setter Property="StrokeThickness" Value="2"/>
                    <Setter Property="Fill" Value="Transparent"/>
                </Style>
            </Canvas.Resources>

        </Canvas>
    </Grid>
</Page>
